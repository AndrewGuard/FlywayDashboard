In the server folder run

node index.js

In the src folder run

npm start

server\jdbc-connections.json should contain a list of all JDBC connections intended to be used. TODO: improve this to scan a server for flyway DBs
